import 'package:acwadcom/acwadcom_packges.dart';

class RequestScreenAdmin extends StatelessWidget {
  const RequestScreenAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}